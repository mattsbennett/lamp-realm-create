# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Lamp Realm Create' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Lamp Realm Create
  pod 'RealmSwift', '~>10'

  target 'Lamp Realm CreateTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Lamp Realm CreateUITests' do
    # Pods for testing
  end

end
